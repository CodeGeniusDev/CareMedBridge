import type { Testimonial } from "@/types";

/**
 * Verified client testimonials are intentionally omitted until approved material is available.
 * The site uses operational improvement highlights instead of unverified customer quotes.
 */
export const testimonials: Testimonial[] = [];
